# Settings for the code in this repo. Copy to .env — training.py loads it via
# python-dotenv to reach CVAT with the SDK.
#
# Running the CVAT server itself is configured separately, in infra/.env.

# CVAT SDK / CLI access
CVAT_URL=http://localhost:8080
CVAT_USERNAME=admin
CVAT_PASSWORD=changeme
